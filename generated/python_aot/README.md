# uci_aot (PolyXML Python AOT Extension)

High-performance native Python extension module generated by PolyXML, compiled with PyO3 and Rust.

## Building and Installing

```bash
# In active virtual environment:
maturin develop --release

# Or build wheel:
maturin build --release
```

## Quick Start

```python
import uci_aot

# Create instance
obj = uci_aot.<ModelName>()

# Serialize / Deserialize XML
xml_data = obj.to_xml()
restored = uci_aot.<ModelName>.from_xml(xml_data)

# Serialize / Deserialize JSON
json_data = obj.to_json()
restored_json = uci_aot.<ModelName>.from_json(json_data)
```
